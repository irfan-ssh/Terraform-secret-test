data "aws_organizations_organization" "org" {
  # feature_set = "ALL"
}


# resource "aws_organizations_account" "aws1" {
#   name      = "aws1"
#   email     = "${var.email_name}01@${var.email_domain}"
#   role_name = aws_iam_role.role.name
# }

# resource "aws_organizations_account" "aws2" {
#   name      = "aws2"
#   email     = "${var.email_name}02@${var.email_domain}"
#   role_name = aws_iam_role.role.name
# }

resource "aws_organizations_account" "aws3" {
  name      = "aws3"
  email     = "${var.email_name}03@${var.email_domain}"
  role_name = aws_iam_role.role.name
}

resource "aws_organizations_account" "aws4" {
  name      = "aws4"
  email     = "${var.email_name}04@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws5" {
  name      = "aws5"
  email     = "${var.email_name}05@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws6" {
  name      = "aws6"
  email     = "${var.email_name}06@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws7" {
  name      = "aws7"
  email     = "${var.email_name}07@${var.email_domain}"
  role_name = aws_iam_role.role.name
}

resource "aws_organizations_account" "aws8" {
  name      = "aws8"
  email     = "${var.email_name}08@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws9" {
  name      = "aws9"
  email     = "${var.email_name}09@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws10" {
  name      = "aws10"
  email     = "${var.email_name}10@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws11" {
  name      = "aws11"
  email     = "${var.email_name}11@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws12" {
  name      = "aws12"
  email     = "${var.email_name}12@${var.email_domain}"
  role_name = aws_iam_role.role.name
}

resource "aws_organizations_account" "aws13" {
  name      = "aws13"
  email     = "${var.email_name}13@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws14" {
  name      = "aws14"
  email     = "${var.email_name}14@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws15" {
  name      = "aws15"
  email     = "${var.email_name}15@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws16" {
  name      = "aws16"
  email     = "${var.email_name}16@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws17" {
  name      = "aws17"
  email     = "${var.email_name}17@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws18" {
  name      = "aws18"
  email     = "${var.email_name}18@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws19" {
  name      = "aws19"
  email     = "${var.email_name}19@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
# resource "aws_organizations_account" "aws20" {
#   name      = "aws20"
#   email     = "${var.email_name}20@${var.email_domain}"
#   role_name = aws_iam_role.role.name
# }
resource "aws_organizations_account" "aws21" {
  name      = "aws21"
  email     = "${var.email_name}21@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws22" {
  name      = "aws22"
  email     = "${var.email_name}22@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws23" {
  name      = "aws23"
  email     = "${var.email_name}23@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws24" {
  name      = "aws24"
  email     = "${var.email_name}24@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws25" {
  name      = "aws25"
  email     = "${var.email_name}25@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
# resource "aws_organizations_account" "aws26" {
#   name      = "aws26"
#   email     = "${var.email_name}26@${var.email_domain}"
#   role_name = aws_iam_role.role.name
# }
resource "aws_organizations_account" "aws27" {
  name      = "aws27"
  email     = "${var.email_name}27@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws28" {
  name      = "aws28"
  email     = "${var.email_name}28@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws29" {
  name      = "aws29"
  email     = "${var.email_name}29@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws30" {
  name      = "aws30"
  email     = "${var.email_name}30@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws31" {
  name      = "aws31"
  email     = "${var.email_name}31@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws32" {
  name      = "aws32"
  email     = "${var.email_name}32@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws33" {
  name      = "aws33"
  email     = "${var.email_name}33@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws34" {
  name      = "aws34"
  email     = "${var.email_name}34@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws35" {
  name      = "aws35"
  email     = "${var.email_name}35@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws36" {
  name      = "aws36"
  email     = "${var.email_name}36@${var.email_domain}"
  role_name = aws_iam_role.role.name
}

resource "aws_organizations_account" "aws37" {
  name      = "aws37"
  email     = "${var.email_name}37@${var.email_domain}"
  role_name = aws_iam_role.role.name
}

resource "aws_organizations_account" "aws38" {
  name      = "aws38"
  email     = "${var.email_name}38@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws39" {
  name      = "aws39"
  email     = "${var.email_name}39@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws40" {
  name      = "aws40"
  email     = "${var.email_name}40@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws41" {
  name      = "aws41"
  email     = "${var.email_name}41@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws42" {
  name      = "aws42"
  email     = "${var.email_name}42@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws43" {
  name      = "aws43"
  email     = "${var.email_name}43@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws44" {
  name      = "aws44"
  email     = "${var.email_name}44@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws45" {
  name      = "aws45"
  email     = "${var.email_name}45@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws46" {
  name      = "aws46"
  email     = "${var.email_name}46@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws47" {
  name      = "aws47"
  email     = "${var.email_name}47@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws48" {
  name      = "aws48"
  email     = "${var.email_name}48@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
resource "aws_organizations_account" "aws49" {
  name      = "aws49"
  email     = "${var.email_name}49@${var.email_domain}"
  role_name = aws_iam_role.role.name
}
